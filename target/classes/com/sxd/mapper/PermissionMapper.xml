<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sxd.mapper.PermissionMapper">
    <!-- 根据ID查询权限 -->
    <select id="queryPermissionById" parameterType="int" resultType="com.sxd.pojo.Permission">
        SELECT id, name, url, description, status, created_at AS createdAt, updated_at AS updatedAt
        FROM permission
        WHERE id = #{id}
    </select>
    
    <!-- 查询所有权限 -->
    <select id="queryAllPermissions" resultType="com.sxd.pojo.Permission">
        SELECT id, name, url, description, status, created_at AS createdAt, updated_at AS updatedAt
        FROM permission
        WHERE status = 1
        ORDER BY id
    </select>
    
    <!-- 添加权限 -->
    <insert id="insertPermission" parameterType="com.sxd.pojo.Permission">
        INSERT INTO permission(name, url, description, status, created_at, updated_at)
        VALUES (#{name}, #{url}, #{description}, #{status}, NOW(), NOW())
        <selectKey keyProperty="id" order="AFTER" resultType="int">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>
    
    <!-- 更新权限 -->
    <update id="updatePermission" parameterType="com.sxd.pojo.Permission">
        UPDATE permission
        SET name = #{name},
            url = #{url},
            description = #{description},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    
    <!-- 删除权限 -->
    <delete id="deletePermissionById" parameterType="int">
        DELETE FROM permission WHERE id = #{id}
    </delete>
    
    <!-- 根据角色ID查询权限列表 -->
    <select id="queryPermissionsByRoleId" parameterType="int" resultType="com.sxd.pojo.Permission">
        SELECT p.id, p.name, p.url, p.description, p.status, p.created_at AS createdAt, p.updated_at AS updatedAt
        FROM permission p
        JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND p.status = 1
        ORDER BY p.id
    </select>
    
    <!-- 根据用户ID查询权限列表 -->
    <select id="queryPermissionsByUserId" parameterType="int" resultType="com.sxd.pojo.Permission">
        SELECT p.id, p.name, p.url, p.description, p.status, p.created_at AS createdAt, p.updated_at AS updatedAt
        FROM permission p
        JOIN role_permission rp ON p.id = rp.permission_id
        JOIN user u ON rp.role_id = u.role_id
        WHERE u.id = #{userId} AND p.status = 1
        ORDER BY p.id
    </select>
    
    <!-- 根据URL查询权限 -->
    <select id="queryPermissionByUrl" parameterType="string" resultType="com.sxd.pojo.Permission">
        SELECT id, name, url, description, status, created_at AS createdAt, updated_at AS updatedAt
        FROM permission
        WHERE url = #{url} AND status = 1
    </select>
    
    <!-- 批量删除角色-权限关联 -->
    <delete id="deleteRolePermissionByPermissionIds">
        DELETE FROM role_permission WHERE permission_id IN
        <foreach collection="permissionIds" item="permissionId" open="(" close=")" separator=",">
            #{permissionId}
        </foreach>
    </delete>
</mapper>